# Architecture Decisions

This file is append-only. Never edit existing entries.

| ID    | Decision                                                                                          | Date          |
|-------|---------------------------------------------------------------------------------------------------|---------------|
| D-001 | Domain solochief.app secured June 21 2026 on Cloudflare                                           | 2026-06-21    |
| D-002 | Domain usesolochief.com secured June 21 2026 on Cloudflare                                        | 2026-06-21    |
| D-003 | Stack — Next.js 15 + Supabase + Anthropic API claude-sonnet-4-6 + Railway + Polar + Resend        | 2026-06-21    |
| D-004 | Phase 1 is web command centre only — no WhatsApp until Phase 2                                    | 2026-06-21    |
| D-005 | UK spelling throughout all copy and code comments                                                 | 2026-06-21    |
| D-006 | No Co-Authored-By trailer on any commits — author Frank A. fasathor@gmail.com only               | 2026-06-21    |
| D-007 | RLS enabled on every Supabase table from day one — no exceptions                                  | 2026-06-21    |
| D-008 | AI proposes, system validates, database writes — AI never writes directly to database             | 2026-06-21    |
| D-009 | Unknown is a first-class status — silence never equals completion                                 | 2026-06-21    |
| D-010 | Context package loads before every AI call — no exceptions                                        | 2026-06-21    |
| D-011 | Next.js 16.2.9 installed — proxy.ts used instead of middleware.ts                                 | 2026-06-21    |
| D-012 | Commitment Inventory built as first real feature — core brain of product                          | 2026-06-21    |
| D-013 | Stage changes always logged to commitment_events table                                            | 2026-06-21    |
| D-014 | Permission level auto-suggested based on stage selection                                          | 2026-06-21    |
| D-015 | Workspace auto-created on first login if none exists                                              | 2026-06-21    |
| D-016 | Complete /docs suite created — mandatory Astor Stack build standard                               | 2026-06-21    |
| D-019 | Auth redirect URL derived from NEXT_PUBLIC_APP_URL — never from request.url. Supabase site URL set to https://solochief.app. Callback at /auth/callback. | 2026-06-21    |
| D-020 | Next.js NEXT_PUBLIC_* vars must be passed as Docker build args — they are baked into the bundle at build time not injected at runtime. Standalone output mode used for Railway deployment. | 2026-06-21    |
| D-021 | SoloChief AI is generic and user-configurable. Onboarding templates (solo_founder, freelancer, student_builder, creator, professional, scratch) are suggestions only. No hardcoded product or user names in production logic. | 2026-06-21    |
| D-022 | Intelligence infrastructure built in Week 3 — 5 new tables: pattern_events, weekly_scores, streak_records, memory_references, user_intelligence_state. RLS enabled on all. | 2026-06-21    |
| D-023 | Progressive disclosure — intelligence features revealed based on data sufficiency thresholds, not time. Thresholds: 10 daily logs OR 2 reviews OR 5 switch/park events unlocks pattern voice. 4+ weeks unlocks weekly score and streak visibility. | 2026-06-21    |
| D-024 | Weekly score not shown until user completes at least 1 full weekly plan + Friday review. Before that threshold only Weekly Summary is shown. Score is 0-10, never displayed without sufficient data. | 2026-06-21    |
| D-025 | Streak tracking is quiet and text-only — no emojis, no badges, no fire icons, no gamification. A streak is an accountability signal not a reward mechanism. | 2026-06-21    |
| D-026 | The Honest Silence Rule — when insufficient data exists, AI says nothing meaningful rather than filling space with generic productivity tips. Exact phrase: "I need at least one completed week before I can give you meaningful feedback." | 2026-06-21    |
| D-027 | Onboarding creates the first weekly plan with main_focus_commitment_id set. Profile onboarded_at is set after onboarding completes. Dashboard auto-creates workspace on first login (D-015) so onboarding can always find a workspace. | 2026-06-21    |
| D-028 | Monday Command Centre enforces exactly 3 outcomes before locking. Lock button validates: main focus set + 3 non-empty outcomes. Locked plans cannot be edited. Plan status derived from locked_at: null = draft, non-null = active. | 2026-06-21    |
| D-029 | Switch Challenge — all switch requests logged to switch_requests table before decision. Decision is either approved (urgent reason selected) or blocked (not urgent selected). Both paths log to switch_requests. | 2026-06-21    |
| D-030 | Rescue Me is a sidebar-level modal available on every dashboard page. It provides 5 contextual responses with direct links — does not require AI call for Phase 1. AI Chat link available as one option for complex situations. | 2026-06-21    |
| D-031 | Parking Lot is a soft-queue for ideas that arrive mid-week. Items have status: waiting / scheduled / cleared / killed / actioned. Category: new_product / feature / content / personal / other. Source: web / whatsapp. Parked items reviewed on Monday plan; scheduled items show review_date. | 2026-06-22    |
| D-032 | Follow-ups are tracked with urgency (critical / high / normal / low) and status (open / waiting / completed / cancelled). Soft delete via deleted_at — completed and cancelled items still exist for reporting. Overdue count shown as red badge on sidebar nav item. | 2026-06-22    |
| D-033 | Friday Review has 7 sections: (1) weekly outcome ticks, (2) what shipped, (3) what slipped, (4) wrongly touched work, (5) missed follow-ups, (6) parked ideas, (7) below-level work, plus next-week focus commitment and stop-list change. Completing the review upserts a streak_records row. | 2026-06-22    |
| D-034 | AI Chat uses Anthropic streaming API (anthropic.messages.stream) with SSE. Context package is built before every message — profile, commitments, week plan, stop list, today log, overdue follow-ups, parking lot (last 5), switch requests this week, last review. Both user and assistant messages saved to ai_messages table. Model: claude-sonnet-4-6. | 2026-06-22    |
| D-035 | Command Centre (dashboard index) shows: personalised greeting, no-plan CTA, today snapshot, week-at-a-glance stats, attention items (overdue follow-ups, stale parking lot, inactive launch checklists), quick action grid, and progressive disclosure weekly score card when unlocked. | 2026-06-22    |
| D-036 | Complete UI redesign June 22 2026. Design system in globals.css using sc- prefixed classes. Standard: Linear/Sunsama quality. Tone: calm, operational, direct. No exclamation marks in system copy. Typography: 400 and 500 weight only. Sentence case everywhere. | 2026-06-22    |
| D-037 | Typography scale added to globals.css: sc-page-title (22px/500), sc-page-subtitle (13px/muted), sc-section-heading (11px/600/uppercase), sc-focus-title (22px/500/midnight), sc-value-large (28px/500), sc-meta (11px/muted). All sc- components must use these classes rather than inline font sizes. | 2026-06-22    |
| D-038 | Command Centre rebuilt to three-column card row: Today's Focus, This Week, Needs Attention. Attention items rendered inside card 3 with left-border colour coding (red/amber/purple). Greeting personalised with first name from profiles table. Stats row and quick actions remain below the cards. | 2026-06-22    |
| D-039 | Settings page, Launch Checklists page, and AI Chat welcome state are design-only builds — no server actions wired yet. Settings shows Communication, Schedule, WhatsApp, Account, and Communication mode sections with local state. Persistence will be added in the next build. Today Focus card now has 3px teal left border and sc-focus-title (22px) commitment name. | 2026-06-22    |
| D-040 | Complete design foundation established June 22 2026. globals.css expanded with new tokens (--sc-r, --sc-sidebar-w, --sc-topbar-h, --sc-teal-20, --sc-surface-2, --sc-text-2, --sc-hint, --sc-border-2), backward-compat aliases for renamed tokens, two-column grid system (.sc-grid-main + .sc-context-panel), and new component classes. All existing sc-* classes preserved. Component library created in /src/components/ui/solochief/ (PageShell, ContextPanel, MetricRow, EmptyState). All core screens rebuilt on the system. Mobile responsive at 1024px (single column) and 768px (sidebar hidden). | 2026-06-22    |
| D-041 | "Killed" renamed to "Archived" in all Parking Lot display labels. Database status value remains "killed" — label only changes in STATUS_GROUPS in parking-lot-client.tsx. | 2026-06-22    |
| D-042 | Empty commitment stages collapse to compact sc-stage-empty-row (dashed border, 10px/14px padding, hint colour). Replaces the previous large padded empty block. | 2026-06-22    |
| D-043 | ContextPanel and ContextBlock components built for reuse across two-column screens. Command Centre, Today Focus, and Weekly Plan all use .sc-grid-main (1fr + 300px) layout with sticky context panels. Context panel collapses to single column at 1024px breakpoint. | 2026-06-22    |
| D-044 | No new features until design foundation is confirmed stable. All future screens must be built inside the sc- design system using .sc-grid-main for multi-column layouts, ContextPanel for sticky sidebars, and the established token set. | 2026-06-22    |
| D-045 | Mobile sidebar toggle added. Hamburger button (.sc-mobile-menu-btn, position: fixed) visible at ≤768px. Backdrop overlay (.sc-sidebar-overlay) closes on tap. Sidebar adds .open class when active. Nav links call setMobileOpen(false) on click. Topbar gets padding-left: 52px on mobile to clear the button. | 2026-06-22    |
| D-046 | Hardcoded personal email removed from Settings. Email now loaded via supabase.auth.getUser() in a useEffect inside the client component. Displays "Loading..." placeholder until resolved. | 2026-06-22    |
| D-047 | User-facing "coming in the next build" copy removed from Settings. handleSave toasts now read "saved." only. WhatsApp Connect and Export My Data buttons are disabled with a helper text note. | 2026-06-22    |
| D-048 | Follow-ups false hardcoded metric removed. "Completed this month" stat replaced with "total open" calculated from the live followups array. | 2026-06-22    |
| D-049 | Today Focus switch control made mobile-safe. Hover-only dropdown replaced with click-toggled state (showSwitchMenu). Full-screen invisible backdrop button closes the menu on outside click or tap. Dropdown right-aligned to prevent overflow on narrow viewports. | 2026-06-22    |
| D-050 | Dashboard header pattern standardised across all core pages. Topbar is now minimal chrome (empty left, sc-topbar-actions right reserved for future controls). Each page opens with a PageHeader component (sc-page-header) containing a 24px h1 title, optional subtitle, and optional action/secondaryAction slots. PageHeader component created at /src/components/ui/solochief/PageHeader.tsx. | 2026-06-22    |
| D-051 | Main content spacing improved. PageHeader rendered above sc-grid-main so context panels align with the first content card rather than the topbar. sc-context-panel sticky top updated to calc(topbar-h + 16px). sc-page-title increased to 24px. Mobile: sc-page-header-actions wraps below title at 768px. | 2026-06-22    |
| D-052 | Dashboard topbar converted from empty chrome to global app header. Topbar moved to dashboard layout so it renders once across all pages. Left: current section label. Right: Ask SoloChief link (hidden on AI Chat), Appearance shortcut, Settings shortcut. DashboardTopbar component at /src/components/dashboard/DashboardTopbar.tsx. | 2026-06-22    |
| D-053 | Settings redesigned into a global SaaS settings centre. Two-column layout: 192px sticky left nav + right content area. Six sections: Communication, Schedule, Appearance, WhatsApp, Account, Data & Danger Zone. CSS classes: sc-settings-layout, sc-settings-nav, sc-settings-nav-item, sc-settings-content, sc-settings-card, sc-radio-card. Mobile: nav becomes horizontal scroll strip. | 2026-06-22    |
| D-055 | Dashboard topbar title strengthened for clearer SaaS workspace hierarchy. sc-topbar-workspace: 15px/600/var(--sc-text)/letter-spacing -0.2px. Uses flex:1 with min-width:0 for graceful truncation on mobile. sc-topbar-actions flex-shrink:0 keeps right side pinned. Icon buttons widened to 32px square hit area. | 2026-06-22    |
| D-054 | Appearance setting introduced. Light, Dark, System options persist to localStorage under key sc-theme. On page load the stored preference is applied by adding or removing the .dark class from the document root. Dark sc-* tokens added to the .dark block in globals.css. Full dark mode design is still being refined. | 2026-06-22    |
| D-056 | Web app manifest added at public/manifest.webmanifest. name: SoloChief AI, short_name: SoloChief, start_url: /dashboard, display: standalone, theme_color: #0F1B2D. Icons: PNG at 192/512 (regular and maskable), SVG for any size. Apple touch icon at /apple-touch-icon.png (180×180). | 2026-06-22    |
| D-057 | Service worker at public/sw.js uses safe static caching only. Cache-first for /_next/static/ and /icons/. Network-first for page navigations with offline.html fallback. Never intercepts /api/, /auth/, or cross-origin requests. No auth or user data is ever cached. Registered by ServiceWorkerRegistration client component in root layout. | 2026-06-22    |
| D-058 | InstallPrompt client component shows a dismissible bottom-right banner when the beforeinstallprompt event fires. Captured prompt stored on window._scInstallPrompt so the Settings > Mobile App section can also trigger it. Dismissed state persisted to localStorage under sc-install-dismissed. Added to dashboard layout so it is only shown for authenticated users. | 2026-06-22    |
| D-059 | Settings expanded from 6 to 10 sections. New sections: Focus Rules (switch protection, daily focus limit, Not Today behaviour), AI Behaviour (interpretation mode, advice style, confidence flags), Mobile App (PWA install status and install trigger), Data & Privacy (export + data transparency). Danger Zone split out as its own section (destructive actions only). | 2026-06-22    |
| D-060 | Focus Rules settings control switch protection (Strict / Balanced / Light), daily focus limit (One only / One + override / Flexible), and Not Today behaviour checkboxes. All local state only — persistence wired in a future build when the user_preferences table is added. | 2026-06-22    |
| D-061 | AI Behaviour settings control interpretation mode (Always confirm / Confirm when unsure / Auto when clear), advice style (Direct / Gentle / Minimal), and three confidence-related checkboxes. Local state only for now — will be read by the AI context package once persisted. | 2026-06-22    |
| D-062 | Mobile App settings section shows install status (standalone vs browser), surfaces the install button using the captured window._scInstallPrompt, and includes a Push Notifications placeholder for a future release. WhatsApp nav icon changed from Smartphone to MessageCircle to avoid duplication with Mobile App. | 2026-06-22    |
| D-063 | PWA icons corrected to use existing SoloChief brand assets. Temporary generated icons removed. Manifest references web-app-manifest-192x192.png and web-app-manifest-512x512.png (brand exports). public/apple-touch-icon.png replaced with the 38KB brand asset from public/icons/. public/favicon.ico added at root from brand favicon. | 2026-06-22    |
| D-064 | SoloChief social preview images created using approved brand assets. public/og/solochief-og.png (1200×630), public/og/solochief-twitter.png (1200×630), public/og/solochief-square.png (1200×1200). Generated with scripts/generate-og.mjs using sharp + logo.png icon mark (1254×1254). Midnight background, teal accent, off-white text. | 2026-06-22    |
| D-065 | Open Graph and Twitter Card metadata added to root layout. title: SoloChief AI, description: Your personal Chief of Staff..., url: https://solochief.app, OG image: /og/solochief-og.png, Twitter card: summary_large_image, Twitter image: /og/solochief-twitter.png. locale: en_GB. | 2026-06-22    |
| D-066 | Social preview images regenerated using approved brand assets with corrected font rendering, transparent logo treatment, and improved composition. Switched from SVG text rendering (caused monospace font substitution on Windows) to sharp Pango text API with explicit Arial fontfile. White background stripped from icon via per-pixel luma masking; dark navy areas tinted to white for legibility on midnight background. Copy updated to: SoloChief AI / Command Centre on web. Chief of Staff in WhatsApp. One brain behind both. | 2026-06-22    |
| D-067 | Commitments page upgraded into a commitment map with summary context panel. Two-column layout using sc-grid-main: left column has search, filters, and stage groups; right column has a sticky ContextPanel showing Main Focus / Active / Follow-up / Parked / Total counts and operational guidance. Collapses to single column at 1024px breakpoint. | 2026-06-22    |
| D-068 | Empty commitment stages collapsed into compact inline rows. Stages with zero commitments (in the current filtered view) show only a coloured dot, stage name, and count 0, with no bordered empty box. Populated stages render the full section header and card grid. | 2026-06-22    |
| D-069 | Commitment cards improved with richer operational metadata. Cards use sc-commitment-card with a 3px left border accent keyed to stage colour. Badges use sc-badge colour variants by category and permission level. Next action shown with teal arrow prefix. Due date and last-updated date shown as sc-meta if present. Stage badge removed from card (redundant with section header and left accent). | 2026-06-22    |
| D-070 | Secondary dashboard pages upgraded with guidance panels and stronger empty states. Checklists, Parking Lot, Follow-ups, Friday Review, and AI Chat all use the sc-grid-main two-column layout with ContextPanel on the right. Empty states explain what the user should do next. Tone is calm and operational throughout. | 2026-06-22    |
| D-071 | Parking Lot reinforced as a focus-protection feature. Subtitle: "Ideas captured safely. Not lost — waiting." Explanation text added. Status group labels updated: Parked, Reviewing soon, Cleared, Archived. "Kill idea" renamed to "Archive idea" in all UI. "Archived" was already the display label for the killed status. | 2026-06-22    |
| D-072 | AI Chat upgraded into a guided Chief of Staff assistant interface. Right-side context panel (sc-chat-sidebar, 268px, hidden on ≤1024px) shows today's focus, follow-ups due, weekly progress, and stop list context. Suggested prompts updated to: What should I focus on today?, What can wait?, Park an idea, Add a follow-up, I am overwhelmed, Review my week. Context data fetched server-side in chat/page.tsx. | 2026-06-22    |
| D-073 | Friday Review complete state expanded into a weekly summary experience. Shows: completed outcomes (teal tick), slipped outcomes (muted circle + Slipped badge), shipped text, slipped text, missed follow-ups, parked ideas, energy/focus ratings. Three next-action links: View this week's plan, Start next week, Ask SoloChief. Copy: "Review complete. Use what happened this week to shape the next plan." | 2026-06-22    |
| D-075 | Dashboard page containers centred on wide screens. Added .sc-page-container (max-width: 1240px), .sc-page-container-wide (1320px), .sc-page-container-narrow (920px) to globals.css — all width: 100% with margin: auto. Applied to: Command Centre, Today, Weekly Plan, Commitments, Launch Checklists, Parking Lot, Follow-ups, Friday Review, Settings. AI Chat left as full-height flex layout. sc-content-narrow updated with margin: auto. PageShell updated to use the new container classes. | 2026-06-22    |
| D-076 | AI Chat empty state retitled to "What do you need help deciding?" with subtitle "Ask SoloChief about focus, follow-ups, parked ideas, or what should wait." Context sidebar weekly progress and stop list placeholders updated from "Loaded from plan/settings" to "Not enough data yet" — honest per D-026 Honest Silence Rule. | 2026-06-22    |
| D-077 | Friday Review active-state section labels cleaned from "Section N — ..." prefix to plain human headings: What got done this week?, What did not get done?, What pulled you off plan?, Missed follow-ups, What got parked this week, What was below your level this week?, Next week. Broken link /dashboard/plan corrected to /dashboard/weekly-plan. | 2026-06-22    |
| D-078 | PWA and Mobile App surfaces reviewed for private beta readiness. InstallPrompt copy updated to "Add SoloChief to your home screen for faster daily check-ins." Mobile App push notification copy updated to "Push notifications will be added only after reminder rules are finalised." Data export helper text updated to "Data export is not yet available in private beta." All changes remove future-promise language. | 2026-06-22    |
| D-079 | Private beta tester readiness surfaces added. Dashboard topbar shows a subtle "Private beta" badge in sc-topbar-actions. Settings > Account includes a Private beta card: "SoloChief is currently in private beta. Some features may be refined as the product learns from real usage." with a mailto feedback link to hello@astorstack.com. No "coming soon" or placeholder language remains in user-facing copy. | 2026-06-22    |
| D-080 | Final private beta UI polish. Topbar title strengthened to 16px/600. Sidebar and topbar label "Launch Checklists" renamed to "Checklists" (route unchanged). "Private beta" topbar badge moved to CSS class sc-topbar-beta, hidden at ≤640px to prevent overflow on mobile. Friday Review next-step links given padded row treatment for clear clickability. Parking Lot confirmed: active items show "Parked", closed items show "Archived" (DB status "killed" unchanged). | 2026-06-22    |
| D-081 | Settings mobile navigation improved. On mobile (≤768px) the horizontal left-nav pill strip is hidden and replaced with a collapsible section switcher: a full-width toggle button showing the active section icon and label (with ChevronDown), which expands to a list of all ten sections. Selecting a section closes the menu. Desktop left-nav is unchanged. Classes: sc-settings-mobile-nav, sc-settings-mobile-toggle, sc-settings-mobile-menu, sc-settings-mobile-menu-item. | 2026-06-22    |
| D-082 | user_preferences table added (migration 005). One row per user, upserted on conflict user_id. Fields: checkin_intensity, preferred_channel, communication_mode, timezone, working_day_start/end, quiet_hours_start/end, switch_protection, daily_focus_limit, ai_interpretation, advice_style, show_confidence, theme. RLS policy: auth.uid() = user_id FOR ALL. Trigger: handle_updated_at. Values use underscores throughout (e.g. inapp, one_plus_override, log_automatically). | 2026-06-22    |
| D-083 | Settings page converted to server component + client component split. settings/page.tsx loads preferences and profile server-side, passes as props to SettingsClient. SettingsClient (src/components/settings/settings-client.tsx) is a 'use client' component: state initialised from props with defaults, each section has its own save handler calling upsertPreferences or upsertProfile server actions. Theme persisted to both DB and localStorage for immediate page-load application. firstName/lastName derived from profiles.full_name (split on first space). | 2026-06-22    |
| D-084 | Resend used for all transactional email. From: SoloChief AI &lt;hello@astorstack.com&gt;. Utility at src/lib/email/resend.ts. Templates: welcome, monday-reminder, friday-reminder, overdue-followup, inactivity-nudge (src/lib/email/templates/). RESEND_API_KEY and RESEND_FROM_EMAIL set in .env.local and Railway environment variables. | 2026-06-22    |
| D-085 | Welcome email triggered on onboarding completion (src/lib/actions/onboarding.ts). Sent after intelligence state is initialised, immediately before redirect to dashboard. Fire-and-forget — email failure does not block the redirect. | 2026-06-22    |
| D-086 | Cron API routes for scheduled emails at src/app/api/email/monday-reminder, friday-reminder, overdue-followups. All protected by x-cron-secret header (CRON_SECRET env var). Use service-role admin client (src/lib/supabase/admin.ts) to list auth users and profiles. Cron jobs to be scheduled in Railway or external cron service after beta opens. | 2026-06-22    |
| D-087 | Auth upgraded from magic-link-only to email/password. Magic link kept as secondary sign-in option on the login page. Signup: name + email + password (min 8 chars). Signin: email + password + optional secure email link. Forgot password and reset password pages added at /auth/forgot-password and /auth/reset-password. Callback route updated to redirect to /auth/reset-password on type=recovery. Brand panel extracted to src/components/auth/auth-brand-panel.tsx. Run supabase/migrations/006_profile_trigger.sql in Supabase SQL Editor to capture full_name from signup metadata. | 2026-06-22    |
| D-088 | Settings moved to named sidebar nav item below AI Chat with a separator border-top. Replaces the icon-only settings link that was hidden in the sidebar user row. Consistent with all other named nav items. | 2026-06-22    |
| D-089 | Security section added to Settings (after Account). Change password with current password verification via re-authentication. Magic link sign-in option as secondary method. Password minimum: 8 characters. Success and error feedback inline. | 2026-06-22    |

| D-090 | Onboarding gate strengthened: dashboard layout now checks both commitment count AND profile full_name. If either is missing, user is redirected to /onboarding. Users who have not yet set a name pass through auth but are caught at the dashboard gate. | 2026-06-22    |
| D-091 | Onboarding resume logic added to dashboard layout. If a user has commitments and a name but onboarded_at is null and no weekly_plan exists for the current week, they are redirected to /onboarding?step=3 to complete their weekly setup. Guard on onboarded_at prevents existing completed users from being bounced every Monday. Onboarding page reads ?step= param (server-side searchParams prop) and passes initialStep to OnboardingClient. | 2026-06-22    |
| D-092 | Dashboard weekly-plan CTA contrast improved: sc-plan-cta class with rgba(0,194,168,0.10) background, rgba(0,194,168,0.40) border, --sc-text title, --sc-text-2 description, hover darkens tint. Topbar theme toggle fixed: Sun link replaced with cycle button (Light → Dark → System → Light), Moon and Monitor icons added, shared lib/theme.ts helper created so topbar and Settings Appearance stay in sync via StorageEvent dispatch. | 2026-06-22    |
| D-093 | Polar used for SoloChief billing. Free plan is default — no Polar product needed. Pro ($15/month) and Operator ($24/month) products in Polar. Webhook handles subscription lifecycle events (created, active, updated, canceled, revoked, past_due, uncanceled). Plan stored on profiles.plan column. Commitment limit enforced in createCommitment server action. AI access enforced in AI chat route. Run supabase/migrations/007_billing.sql in Supabase SQL Editor. | 2026-06-23    |
| D-094 | Customer external ID is Supabase user ID. Polar customerExternalId = user.id for webhook matching. polar_customer_id stored on profile after first subscription. Webhook uses createAdminClient (service role) to bypass RLS for plan updates. | 2026-06-23    |
| D-095 | Settings navigation reordered around product behaviour, channels, and account/trust sections. Communication now leads Settings and Billing moved below Account. Sidebar groups separated by a faint 0.5px border-top with 12px spacing. | 2026-06-23    |
| D-096 | Polar post-payment flow updated to use a dedicated billing success experience at /dashboard/billing/success. Checkout successUrl points to this route. customer_session_token is stripped from the URL on mount. Success page reads plan from DB and polls every 3 seconds (max 10 attempts) until webhook activates the plan. Confirms active plan, lists unlocked features, and guides paid users to next SoloChief actions. | 2026-06-23    |
| D-097 | Billing settings screen polished into a structured SaaS billing centre with current plan, included features, subscription actions, upgrade path, and trust messaging. plan_expires_at and plan_cancelled_at fetched from profiles and threaded through SettingsClientProps for renewal and cancellation display. | 2026-06-23    |
| D-098 | Sign-in UX split into password and secure email-link modes so email-link sign-in uses a dedicated single-field flow with calm validation and success states. Mode switch clears all errors and re-focuses the email field. Field-level validation replaces global red messages. Auth failure copy is calm and actionable. | 2026-06-23    |
| D-099 | Secure email sign-in flow now supports both email link and manual verification code entry, with resend and calm error states. verifyOtp with type='email' used for code verification. Code input strips non-digits, uses inputMode=numeric and autoComplete=one-time-code for mobile. | 2026-06-23    |
| D-100 | Security settings now distinguish password accounts from secure-email-link-only accounts, showing Set password for magic-link users and Change password for password users. hasPasswordProvider derived from user.identities in the server component and passed as a prop. After setting a password, client state updates immediately so the section transitions to Change password without a page reload. | 2026-06-23    |
| D-111 | WhatsApp onboarding state machine added. Four-step flow: (1) consent (reply 1 or 2), (2) quiet hours preference (reply 1-4), (3) briefing time (reply 1-4), (4) complete. Migration 014 adds whatsapp_onboarding_step, whatsapp_consent_at, whatsapp_quiet_start/end, whatsapp_briefing_hour, whatsapp_onboarded_at to profiles. OTP verification triggers startOnboarding, sending consent message. Webhook routes mid-onboarding users to handleOnboardingReply. 'setup' command restarts onboarding. sendMorningBriefings and sendFollowupNudges respect quiet hours and briefing_hour preferences, skip users with no whatsapp_onboarded_at. | 2026-06-25    |
| D-101 | Platform admin panel added at /admin with PLATFORM_ADMIN_EMAILS access control (comma-separated env var). Layout protects all /admin/* routes server-side via isPlatformAdmin helper. Seven sections: Dashboard (metrics, activation funnel, recent activity), Users (searchable table + detail view), Billing (plan table with status), Email (test email sender + cron triggers), Beta (empty state), Feedback (empty state), System (integration status, DB counts, cron triggers). Admin API routes at /api/admin/* are independently gated server-side. Separate admin shell with midnight sidebar and clean topbar, no dashboard layout coupling. | 2026-06-23    |
| D-102 | Admin v2 backlog documented. No code changes — second admin email (hello@astorstack.com) added via Railway PLATFORM_ADMIN_EMAILS env var only; isPlatformAdmin comma-separated parsing already supports this. Admin v2 improvements tracked below: (1) Audit log — every manual admin action should record admin email, action, target user/email, timestamp, result. (2) Real feedback backend — feedback table with message, type, status (New/Reviewed/Actioned/Closed), reviewed date, reviewed by. (3) Admin search polish — users searchable by email, name, plan, onboarding status with stable filter state. (4) System health maturity — System page to add webhook status, last cron run, last email sent, last failed email, last Polar event. Rule: never display raw secrets or tokens, only configured/missing status and safe operational timestamps. | 2026-06-23    |
| D-103 | Admin users now see an Admin sidebar link (ShieldCheck icon + INT badge) pointing to /admin. isAdmin boolean derived from isPlatformAdmin(user.email) in the server dashboard layout and passed as a prop to SidebarNav. Client-side link is UX only; server-side gate in /admin/layout.tsx remains the real security control. Secure email sign-in hardened: rate-limit OTP errors now show code entry with an inbox check message rather than silently passing; stale code warning shown after resend ("Use the latest code we sent. Older codes may no longer work."); auth/callback logs exchange failures for production debugging. | 2026-06-23    |
| D-104 | Admin layout polished with centred max-width content containers, balanced user detail grids, aligned topbar/content spacing, and responsive table behaviour for a more global SaaS operations-console feel. AdminShell main area now uses .admin-content class (max-width 1200px, margin 0 auto, padding 28px 32px). AdminTopbar inner wrapper aligns to same max-width. User detail page Profile and Billing cards sit side-by-side in a responsive 2-column grid rather than a narrow left-aligned stack. Padding reduces to 20px/16px on mobile. | 2026-06-23    |
| D-105 | Email sender, subject rendering, inactivity nudge template, user feedback capture, admin feedback listing, and admin table pagination were polished for global SaaS readiness. (1) All emails now send from support@astorstack.com (RESEND_FROM_EMAIL env var, default updated). SUPPORT_EMAIL constant exported from src/lib/email/resend.ts and imported by all templates — no duplicate strings. (2) Email subject HTML entities fixed: &#8212; replaced with plain em dash — in all subjects; plain hyphens used in plain text bodies. (3) Inactivity nudge template updated with new subject ("Still want SoloChief to help you stay on track?") and calmer body copy ("SoloChief works best when your commitments stay visible. Open your dashboard and choose one thing that deserves attention today."). (4) User feedback form added at /dashboard/feedback (FeedbackForm client component, submitFeedback server action, RLS-enforced INSERT into feedback table). Feedback link added to user sidebar near Settings. (5) Admin feedback page replaced with real data: status filter tabs (All/New/Reviewed/Actioned/Closed), paginated table (10 rows), Mark reviewed and Close server actions with revalidatePath. (6) Admin Users and Billing pages paginated at 10 rows per page using AdminPagination component with prev/next links that preserve existing filters. AdminFilters resets page to 1 on filter change. (7) Admin sidebar "Main dashboard" link added above the signed-in email footer for easy return to /dashboard. Migration 008_feedback.sql creates feedback table with RLS. | 2026-06-23    |
| D-107 | Full end-to-end audit completed (2026-06-24). Build: next build clean (41 pages, 0 errors). TypeScript: tsc --noEmit clean. Cron security: all three POST /api/cron/* routes return 401 without x-cron-secret header and 405 on GET — confirmed correct. GitHub Actions workflows active on main branch (cron-monday-plan, cron-friday-review, cron-followup-reminders). One bug found and fixed: src/app/admin/layout.tsx line 12 had redirect('/auth/signin') — route does not exist; corrected to redirect('/auth/login'). All other flagged items (streak comparison in reviews.ts, redundant .then() in scheduled-emails.ts, onboarding silent writes) assessed as non-bugs: streak logic is safe on UTC (Railway default), .then() is harmless, onboarding writes are intentionally non-fatal. | 2026-06-24    |
| D-109 | MDP Agent Trust Engine implemented (2026-06-24). Four built-in agents (planning, focus, followup, review) each hold an independent trust state per user. States: candidate → proving → valued; void is terminal (shown as "Rebuilding" in UI). Transition matrix: R1 (correct signal): candidate→proving, proving→valued at streak≥3, valued stays valued; R0 (incorrect): valued→proving, proving→candidate, candidate→void at r0_count≥5. Void has no exit — terminal. (1) Migration 013_agent_mdp_states.sql: table with state, correct_streak, r0_count, total_evaluations, last_evaluated_at; RLS user-scoped; seed trigger on_user_created_seed_mdp fires on signup; backfill inserts rows for all existing users. Run manually in Supabase SQL Editor. (2) src/lib/actions/mdp.ts: evaluateAgent(userId, agentName, correct) — full transition matrix, void guard, admin client writes; getUserAgentStates(userId); stateLabel() helper (never exposes "void" to callers). (3) src/lib/actions/mdp-signals.ts: collectPlanningSignal (≥60% outcomes R1, <30% R0), collectFocusSignal (null — Switch Challenge not yet built), collectFollowupSignal (resolved>0 → R1; overdue≥3 → R0), collectReviewSignal (completed + avg rating ≥3 → R1). (4) Friday Review integration: completeReview in reviews.ts collects all four signals and calls evaluateAgent for each after successful save; MDP errors are caught and logged but never block the review save. (5) AgentTrustPanel component at src/components/mdp/AgentTrustPanel.tsx: read-only, shows state badge, progress dots, streak, total evaluations, last signal date per agent. Added to /dashboard/settings under Agent Trust section in Account & trust group. Settings page loads agent states via getUserAgentStates. (6) Admin: getAgentMdpStats() in admin.ts returns byState counts, allValuedUsers, anyVoidUsers, lastEvaluatedAt; Agent Trust Engine section added to admin/system/page.tsx. User-facing language: never "void" or "dead" — always "Rebuilding". | 2026-06-24    |
| D-107 | Full end-to-end audit completed (2026-06-25). Build: next build clean (44 pages, 0 errors). TypeScript: tsc --noEmit clean. Cron security: all three POST /api/cron/* routes return 401 without x-cron-secret header and correct response format with secret. Routes verified: 42 pages and API endpoints all present. Auth pages: signup/login forms properly structured with email/password inputs. API security: chat requires auth (401), billing webhook validates signatures (403 on invalid). Admin routes correctly redirect unauthenticated users (307). Database: all 13 migrations present (001-013, including email logs D-106, MDP D-109, WhatsApp D-108). Error handling: Supabase 500 errors in signup form handled gracefully with user-friendly message. No secrets exposed in any error responses. Verified flows: cron job processing, auth redirects, database schema. Flows not tested (require browser automation): full signup/email confirmation, onboarding completion, dashboard workflows, billing, AI Chat, WhatsApp. Assessment: PASS — infrastructure, security, and build integrity verified. Ready for user testing. | 2026-06-25    |
| D-108 | WhatsApp Phase 2 shipped (2026-06-24). Full channel: inbound webhook, OTP user linking, outbound morning briefings and follow-up nudges, command routing, AI fallback. (1) Three DB migrations: 010 adds whatsapp_number + whatsapp_verified to profiles and whatsapp_notifications_enabled to notification_preferences; 011 creates whatsapp_otps table (SHA-256 hashed, 15-min expiry, one-per-user invalidation); 012 creates whatsapp_logs table (direction, type, dedupe_key UNIQUE, status, error). Run migrations manually in Supabase SQL Editor. (2) src/lib/whatsapp/twilio.ts: Twilio client factory, sendWhatsApp(), validateTwilioSignature() (using twilio npm validateRequest), twimlResponse(), twimlEmpty(), maskPhone(). (3) src/lib/actions/whatsapp.ts: server actions — sendWhatsAppOtp, verifyWhatsAppOtp, unlinkWhatsApp, toggleWhatsAppNotifications, getWhatsAppStatus. OTP never stored in plain text. (4) src/lib/whatsapp/scheduled-whatsapp.ts: sendMorningBriefings(), sendFollowupNudges(), buildAiReply() (claude-haiku-4-5-20251001, max 200 tokens). Deduplication via whatsapp_logs.dedupe_key. Opt-out respected via notification_preferences. (5) POST /api/whatsapp/webhook: Twilio X-Twilio-Signature validation gate (403 on failure); command routing (hi/briefing/focus/follow-ups/commitments/plan/capture/done/help); AI fallback for unrecognised input. Message body not logged — type and status only. (6) POST /api/cron/whatsapp-morning-briefing and /api/cron/whatsapp-followup-nudge: x-cron-secret protected, return {ok, sent, skipped, failed}. (7) Settings UI: OTP flow UI (not_connected → entering_phone → otp_sent → connected), masked number display (first 3 + last 4 digits), Remove button, notifications toggle. (8) Admin: whatsappVerifiedCount added to SystemStatus; WhatsApp section on system page; masked whatsapp_number + whatsapp_verified on user detail page. Security constraints: no secrets in URLs, no GET on cron routes, no plain-text OTPs stored, no message bodies logged, no full phone numbers in admin UI. Twilio sandbox: +1 415 523 8886, join machine-spin. | 2026-06-24    |
| D-106 | Scheduled automated emails added for Monday planning, Friday review, and due/overdue follow-ups. (1) Email log table (migration 009_email_logs.sql) with dedupe_key UNIQUE constraint for duplicate-send prevention. Types: monday_plan, friday_review, followup_due, inactivity_nudge. Status: sent, failed, skipped. (2) Reusable scheduled-email functions (src/lib/email/scheduled-emails.ts): sendMondayPlanEmails, sendFridayReviewEmails, sendFollowupReminderEmails. All use createAdminClient (service role) for auth user listing and profile fetches. Deduplication via email_logs. Respect notification_preferences.email_enabled. Check weekly plan status before sending Monday email. Check review completion status before sending Friday email. Follow-up email checks for due_date ≤ today and splits overdue from due today. Max 5 items shown per email with "and X more" fallback. (3) New follow-up reminder email template (src/lib/email/templates/followup-reminder.ts) with dynamic subject logic and colour-coded HTML sections (red for overdue, blue for due today). (4) Three new cron routes: POST /api/cron/monday-plan-email, /api/cron/friday-review-email, /api/cron/followup-reminders-email. All protected by x-cron-secret header. (5) Admin trigger route (/api/admin/trigger-cron/[type]) refactored to call scheduled-email functions directly instead of proxying to old /api/email routes. New type names: monday-plan-email, friday-review-email, followup-reminders-email. (6) Admin Email page (AdminEmailPanel) updated to accept AdminEmailStats props and display configured status and last-sent timestamps for each scheduled job. (7) Admin System page enhanced with email delivery stats section: total sent, total failed, last run times for Monday/Friday/follow-up, last failed email timestamp. All timestamps safe (no secrets). (8) getAdminEmailStats() added to admin.ts for graceful email_logs table queries (safe fallback if table doesn't exist). (9) UK spelling throughout. Timezone delivery documented as future work. | 2026-06-23    |
| D-115 | Voice note support on WhatsApp (2026-07-08). Users send voice notes via WhatsApp; Chief transcribes using OpenAI Whisper API and processes the transcription as a text command through normal routing. (1) New package: openai npm module; OPENAI_API_KEY env var added to Railway. (2) New file: src/lib/whatsapp/voice.ts with transcribeVoiceNote(mediaUrl, twilioAccountSid, twilioAuthToken) — fetches audio from Twilio with basic auth, sends to Whisper API, returns transcribed string or null on error. (3) Webhook updated: MediaUrl0 and MediaContentType0 parsed from params; isVoiceNote flag set if NumMedia > 0 and content type includes audio/ogg/mpeg; message check allows isVoiceNote through even with no bodyText. Voice note flow: send acknowledgement ("Got your voice note. Give me a moment..."), transcribe, send confirmation ("I heard: [text]. Processing..."), set bodyText = transcribedText, fall through to normal command routing. (4) AI system prompt updated with voice transcription context: "Users may send voice notes that have been transcribed. Transcriptions may contain minor errors or informal speech. Interpret the intent charitably and respond naturally. If the user says something like 'remind me to call John' or 'note that the meeting moved', treat it as a capture request and confirm what you saved." (5) Cost: ~$0.001 per voice note (negligible at current scale); ~$0.006 per minute of audio. (6) Flow verified: voice messages routed through capture, done, focus, briefing, help, and AI fallback — no special handler needed, same command routing as text. | 2026-07-08    |
