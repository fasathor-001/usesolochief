name: Cron — Monday plan email

on:
  # 06:00 UTC every Monday = 08:00 SAST
  schedule:
    - cron: '0 6 * * 1'
  # Manual trigger for acceptance testing
  workflow_dispatch:

jobs:
  run:
    name: POST /api/cron/monday-plan-email
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Call cron endpoint
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          response=$(curl -s -o /tmp/body.txt -w '%{http_code}' \
            -X POST \
            -H "x-cron-secret: ${CRON_SECRET}" \
            -H "Content-Type: application/json" \
            https://solochief.app/api/cron/monday-plan-email)

          body=$(cat /tmp/body.txt)
          echo "HTTP status : ${response}"
          echo "Response    : ${body}"

          if [ "${response}" != "200" ]; then
            echo "ERROR: expected 200, got ${response}"
            exit 1
          fi
